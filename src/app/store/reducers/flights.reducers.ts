import { Action } from '@ngrx/store';
import { ActionTypes } from '../actiontypes/flight.actions';

const initialState = null;
export flightReducer(state = initialState, action: Action) {
  switch(action) {

  }
}